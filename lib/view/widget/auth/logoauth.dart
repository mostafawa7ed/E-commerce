import 'package:flutter/material.dart';

import '../../../core/constant/imageassets.dart';

class LofgoAuth extends StatelessWidget {
  const LofgoAuth({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Image.asset(ImageAssets.logoauth);
  }
}
